# Minimal production requirements for Render (Python 3.12)
# Includes only packages actually imported by the running Flask app

gunicorn
Flask
flask-cors
python-dotenv
requests>=2.32.3
psutil
edge-tts
pygame
Pillow
selenium
webdriver-manager
mtranslate
groq
cohere
googlesearch-python
beautifulsoup4
keyboard
mss
opencv-python
pywhatkit
yt-dlp
supabase
psycopg2-binary
PyQt5
# Notes:
# - 'requests' pinned to >=2.32.3 to resolve instructor/request conflict.
# - 'psycopg2-binary' is used for PostgreSQL on Render.
# - Optional/OS-specific packages are included if referenced (e.g., keyboard, pygame, cv2).
